version: 2

sources:
  - name: transform_air
    database: "{{ target.database }}"
    schema: "{{ target.schema }}"

    tables:
      - name: bookings

      - name: customers

      - name: flights

models:
  - name: src_transform_air__bookings
    config:
      materialized: view

  - name: src_transform_air__customers
    config:
      materialized: view

  - name: src_transform_air__flights
    config:
      materialized: view
    