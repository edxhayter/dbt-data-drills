version: 2

sources:
  - name: fresh_select
    database: TIL_PORTFOLIO_PROJECTS
    schema: ED_HAYTER
    tables:
      - name: source_status__test
        loaded_at_field: load_timestamp
        freshness:
          filter: datediff('day', load_timestamp, current_timestamp) < 2
      - name: dim_name